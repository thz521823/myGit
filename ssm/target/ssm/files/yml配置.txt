yml 通过@ConfigurationProperties注入值

 package com.ling.entity：
	@Conponent 将javabean注入spring容器中
	@ConfigurationProperties（prefix="student"）//需在pom中添加依赖           	<dependency>
  	public class Student{ }          						<groupId>org.springframework.boot</groupId>
           									 <artifactId>spring-boot-configuration-processor</artifactId>
yml：student:空格								 </dependency>
	
基本类型：属性：空格 值
	map与对象：空格{属性：空格值，}
	list与数组：空格[属性：空格值]     
同一@Value 可以单个注解
public class Student(){
	@Value{"zs"}
	String name;
}

1.@PropertySource:默认会加载application.prooperties/application.yml文件中的数据
	例如：@PropertySource(value={"classpath:conf.properties"})加载conf.properties文件中的数据；
	但是，@PropertySource只能加载properties文件，不能加载yml。
 
//配置类
2.spring boot 推荐用注解方式进行配置：写类，@Configuration @Bean;
@Configuration
public StudentService stuService(){    //方法名，相当于<bean id= “xxx”
	StudentService stuService = new StudentService();
	return stuService;   //返回值<bean class="xxxxx"
}

 					                     	 